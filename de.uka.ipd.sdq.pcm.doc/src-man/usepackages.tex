%
% This file has been generated by UML to LaTeX written in oAW Xpand
% It is save to alter this file as it WILL NOT be overwritten.
% The file is included by the main latex file in the appropriate place, not further
% actions are required
%

% Setup page dimensions
	\setlength\headheight{90pt}
	\usepackage[a4paper,inner=2cm,outer=2.5cm,top=1cm,bottom=2cm,includeheadfoot]{geometry}

% Set the input encoding
%	\usepackage[utf8]{inputenc}
	
% Setup TOC depth (up to level 3, i.e., subsubsections
	\setcounter{secnumdepth}{3}

% Define some makros	
	\usepackage{xspace}
	
	\newenvironment{ChapterList}[1]
		{\begin{list}{}%
			{\renewcommand\makelabel[1]{\bfseries{##1:}\hfill}%
			\settowidth\labelwidth{\makelabel{#1}}%
			\setlength\leftmargin{\labelwidth+\labelsep}}}%
		{\end{list}}
		
% Index generation
	\usepackage{makeidx}
	\makeindex
	
% Uncomment the following line to display overfull boxed
	% \overfullrule=5pt

% Import AMS packages for mathematic typesetting
	\usepackage{amsmath}
	\usepackage{amsfonts}
	\usepackage{amssymb}

% Setup fonts
	% Use T1 fontencoding
	\usepackage[T1]{fontenc}
	% Change roman style to Times New Roman
	%\usepackage{txfonts}
	\usepackage{mathptmx}
	% Use Arial as default Sans font
	\usepackage[scaled=.92]{helvet}
	% Uncomment the next line to make Sans the default font for the whole document
	% \renewcommand*\familydefault{\sfdefault} %% Only if the base font of the document is to be sans serif
	% nicer monospaced font
	\usepackage[scaled]{luximono}

% Setup headings
	\usepackage{titlesec}
	\titlespacing{\chapter}{0pt}{0pt}{*2}
	\titleformat{\chapter}[hang]{\LARGE\bfseries\sffamily}{\thechapter}{0.5em}{}
	\titleformat{\section}[hang]{\Large\bfseries\sffamily}{\thesection}{0.5em}{}
	\titleformat{\subsection}[hang]{\large\bfseries\sffamily}{\thesubsection}{0.5em}{}
	\titleformat{\subsubsection}[hang]{\normalsize\bfseries\sffamily}{\thesubsubsection}{0.5em}{}
	
% Import graphicx for EPS handling
	\usepackage{graphicx}

% Autogenerate a label on the last page
	\usepackage{lastpage}

% Tabular formating packages
	\usepackage{array}
	\usepackage{multirow}
	\usepackage{tabularx}
	\renewcommand\tabularxcolumn[1]{m{#1}}
	
% Package for Hyperrefs in PDF output
	\usepackage[colorlinks=true,linkcolor={blue},citecolor={blue},urlcolor={blue},bookmarksopen]{hyperref}
%\usepackage[colorlinks=true,linkcolor={black},citecolor={black},urlcolor={black},bookmarksopen]{hyperref}

% Listing package for formating OCL expressions
	\usepackage{listings}
  \lstset{language=[decorative]OCL,basicstyle=\small\ttfamily,showspaces=false,showstringspaces=false,breaklines=true,breakindent=10pt,
	        prebreak=\mbox{\tiny$\searrow$},
	        keywordstyle=\color{blue}\bfseries,
	        postbreak=\mbox{{\color{blue}\tiny$\rightarrow$}}}

% Long tables spanning over multiple pages
  \usepackage{longtable}

% Glossary
  \usepackage[style=altlist,toc]{glossary}
  \makeglossary

% Color
	\usepackage{colortbl}
	\usepackage{color}
	
% Hyphenation control
	\usepackage{hyphenat}
